import {TextIconAlignDirective} from "./text-icon-align.directive";

describe('H3IconAlignDirective', () => {
  it('should create an instance', () => {
    const directive = new TextIconAlignDirective();
    expect(directive).toBeTruthy();
  });
});
