import { RemoveSelfPipe } from './remove-self.pipe';

describe('RemoveSelfPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSelfPipe();
    expect(pipe).toBeTruthy();
  });
});
