import { H3IconAlignDirective } from './h3-icon-align.directive';

describe('H3IconAlignDirective', () => {
  it('should create an instance', () => {
    const directive = new H3IconAlignDirective();
    expect(directive).toBeTruthy();
  });
});
