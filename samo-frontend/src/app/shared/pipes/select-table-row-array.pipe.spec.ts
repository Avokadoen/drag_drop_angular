import { SelectTableRowArrayPipe } from './select-table-row-array.pipe';

describe('SelectTableRowArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectTableRowArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
